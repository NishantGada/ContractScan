SUPABASE_URL=
SUPABASE_SERVICE_KEY=       # Service role key — never expose this to frontend
GEMINI_API_KEY=
ALLOWED_ORIGINS=http://localhost:5173
USE_MOCK_GEMINI=false       # true = skip Gemini API, return fixed sample clause risks (dev/testing only)
